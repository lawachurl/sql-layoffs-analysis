# IT Layoffs: очистка и разведочный анализ данных (SQL)

Репозиторий содержит два SQL-скрипта, которые последовательно обрабатывают датасет об увольнениях в IT‑компаниях (2020–2024).  

**Задачи**:
1. Приведение сырых данных к чистому, аналитически пригодному виду.
2. Исследование закономерностей и расчёт ключевых метрик.

Используется **MySQL**, задействованы *оконные функции, CTE, self-join, агрегация, работа со строками и датами*.

Исходный датасет — [Tech Layoffs 2020–2023](https://www.kaggle.com/datasets/swaptr/layoffs-2022).  

---

## 📁 Состав репозитория

```
sql-layoffs-analysis/
│
├── README.md
├── .gitignore
│
├── sql/
│ ├── 01_data_cleaning.sql – очистка и подготовка данных
│ └── 02_exploratory_analysis.sql – разведочный анализ
│
└── data/
└── layoffs_raw_sample.csv – пример исходных данных (20 строк)
```

## 🔧 Описание скриптов

### 01_data_cleaning.sql
1. Создание рабочей таблицы `layoffs_staging` – копия исходной.
2. Удаление точных дубликатов с помощью `ROW_NUMBER()`.
3. Стандартизация полей:
   - обрезка пробелов (`TRIM`),
   - приведение вариантов отраслей к единому виду (`Crypto%` → `Crypto`),
   - исправление названий локаций (Düsseldorf → Dusseldorf и т.п.),
   - удаление точки в названии страны (`United States.` → `United States`).
4. Преобразование строковой даты в тип `DATE` (`STR_TO_DATE`).
5. Обработка пропусков:
   - заполнение `industry` через self-join по компании.
6. Удаление записей без данных об увольнениях.
7. Удаление технического поля `row_num`.

Результат – таблица `layoffs_staging2`.

### 02_exploratory_analysis.sql
1. Максимальные значения уволенных и доли увольнений.
2. Компании с полной ликвидацией (`percentage_laid_off = 1`).
3. Суммарные увольнения по компаниям, отраслям, странам, стадиям финансирования.
4. Динамика по годам и месяцам, накопленный итог (**rolling total**) через **оконную функцию**.
5. Ранжирование компаний по сумме увольнений внутри каждого года (`DENSE_RANK` + **CTE**) – топ-5 по годам.

---

## 📊 Основные выводы

- Пик увольнений – **2022–2023**.
- Наиболее пострадавшие отрасли: **Consumer, Retail, Transportation**.
- Страна-лидер по абсолютному числу сокращений – **США**.
- Крупнейшие увольнения: **Meta, Amazon, Google, Microsoft**.
- Amazon входила в топ-5 три года (2023 — потенциально) подряд.
